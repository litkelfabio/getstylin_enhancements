import { CityStatePipe } from './city-state.pipe';

describe('CityStatePipe', () => {
  it('create an instance', () => {
    const pipe = new CityStatePipe();
    expect(pipe).toBeTruthy();
  });
});
