import { TimeAgoV2Pipe } from './time-ago-v2.pipe';

describe('TimeAgoV2Pipe', () => {
  it('create an instance', () => {
    const pipe = new TimeAgoV2Pipe();
    expect(pipe).toBeTruthy();
  });
});
