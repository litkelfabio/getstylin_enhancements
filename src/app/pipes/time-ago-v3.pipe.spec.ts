import { TimeAgoV3Pipe } from './time-ago-v3.pipe';

describe('TimeAgoV3Pipe', () => {
  it('create an instance', () => {
    const pipe = new TimeAgoV3Pipe();
    expect(pipe).toBeTruthy();
  });
});
